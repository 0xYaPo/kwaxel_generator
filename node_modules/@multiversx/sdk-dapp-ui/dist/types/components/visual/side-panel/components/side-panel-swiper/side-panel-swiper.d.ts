import type { EventEmitter } from '../../../../../stencil-public-runtime';
export declare class SidePanelSwiper {
    sidePanelSwipeElement: HTMLElement;
    open: boolean;
    sidePanelIdentifier: string;
    sheetDismiss: EventEmitter<void>;
    sheetSnapChange: EventEmitter<{
        index: number;
        snapPoint: string;
    }>;
    isVisible: boolean;
    isDragging: boolean;
    currentSnapIndex: number;
    private snapPointsArray;
    private sheetElement;
    private dragState;
    componentDidLoad(): void;
    openChanged(newValue: boolean): void;
    openToSnapPoint(snapIndex?: number): Promise<void>;
    close(): Promise<void>;
    private animateToPosition;
    private animateToClose;
    private handleDragStart;
    private handleDragMove;
    private handleDragEnd;
    private getCurrentTransform;
    render(): any;
}
