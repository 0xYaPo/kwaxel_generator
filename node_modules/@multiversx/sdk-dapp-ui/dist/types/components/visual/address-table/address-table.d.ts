import type { EventEmitter } from '../../../stencil-public-runtime';
import type { IAddressTableData } from "../../../types/address-table.types";
export declare class AddressTable {
    accountScreenData: IAddressTableData;
    selectedIndex: number;
    accessWallet: EventEmitter;
    selectAccount: EventEmitter;
    pageChange: EventEmitter<number>;
    handleAccessWallet(event: MouseEvent): void;
    handleSelectAccount(accountDerivationIndex: number): (event: MouseEvent) => void;
    private handlePageChange;
    private processLedgerAddressIndex;
    render(): any;
}
