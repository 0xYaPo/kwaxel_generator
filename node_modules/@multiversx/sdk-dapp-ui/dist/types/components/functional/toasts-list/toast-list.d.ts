import type { IEventBus } from "../../../utils/EventBus";
import type { CustomToastType, ITransactionToast } from './components/transaction-toast/transaction-toast.type';
export declare class ToastList {
    private readonly eventBus;
    private unsubscribeFunctions;
    private readonly connectionMonitor;
    transactionToasts: ITransactionToast[];
    customToasts: CustomToastType[];
    isVisible: boolean;
    getEventBus(): Promise<IEventBus>;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    private readonly handleHide;
    private readonly handleShow;
    private readonly handleCustomToastDelete;
    private readonly handleTransactionToastDelete;
    private readonly handleViewAllClick;
    private readonly transactionToastUpdate;
    private readonly customToastsUpdate;
    private resetState;
    render(): any;
}
