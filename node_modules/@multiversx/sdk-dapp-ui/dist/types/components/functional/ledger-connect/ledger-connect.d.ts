import { type IEventBus } from "../../../utils/EventBus";
import type { ILedgerConnectPanelData } from './ledger-connect.types';
export declare class LedgerConnect {
    private readonly eventBus;
    private readonly connectionMonitor;
    hostElement: HTMLElement;
    data: ILedgerConnectPanelData;
    private selectedIndex;
    private selectedAddress;
    ledgerDataState: ILedgerConnectPanelData;
    handleDataChange(newValue: ILedgerConnectPanelData): void;
    getEventBus(): Promise<IEventBus>;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    private selectAccount;
    private dataUpdate;
    private accessWallet;
    private handleIntroConnect;
    private handleClose;
    render(): any;
}
