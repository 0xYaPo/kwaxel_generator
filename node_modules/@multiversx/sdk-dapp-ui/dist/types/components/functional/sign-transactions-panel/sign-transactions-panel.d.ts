import type { IEventBus } from "../../../utils/EventBus";
import type { IOverviewProps } from './sign-transactions-panel.types';
import { TransactionTabsEnum } from './sign-transactions-panel.types';
export declare class SignTransactionsPanel {
    private readonly eventBus;
    private unsubscribeFunctions;
    private readonly connectionMonitor;
    closeWithAnimation(): Promise<unknown>;
    isOpen: boolean;
    activeTab: TransactionTabsEnum;
    getEventBus(): Promise<IEventBus>;
    componentWillLoad(): void;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    private readonly handleClose;
    private readonly dataUpdate;
    private setActiveTab;
    private readonly handleBack;
    get overviewProps(): IOverviewProps;
    render(): any;
}
