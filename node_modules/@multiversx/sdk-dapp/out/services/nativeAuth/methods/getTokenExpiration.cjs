"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getTokenExpiration_exports = {};
__export(getTokenExpiration_exports, {
  getTokenExpiration: () => getTokenExpiration
});
module.exports = __toCommonJS(getTokenExpiration_exports);
var import_getUnixTimestamp = require("../../../utils/dateTime/getUnixTimestamp");
var import_decodeNativeAuthToken = require("../helpers/decodeNativeAuthToken");
const notFound = {
  isExpired: false
};
const getTokenExpiration = (token) => {
  if (!token) {
    return notFound;
  }
  const decodedToken = (0, import_decodeNativeAuthToken.decodeNativeAuthToken)(token);
  if (!decodedToken) {
    return notFound;
  }
  const unixNow = (0, import_getUnixTimestamp.getUnixTimestamp)();
  const { ttl, extraInfo } = decodedToken;
  const timestamp = extraInfo?.timestamp;
  if (!timestamp) {
    return notFound;
  }
  const expiresAt = timestamp + ttl;
  const isExpired = unixNow > expiresAt;
  const secondsUntilExpires = expiresAt - unixNow;
  return { isExpired, expiresAt, secondsUntilExpires };
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getTokenExpiration
});
//# sourceMappingURL=getTokenExpiration.cjs.map
