"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getDefaultNativeAuthConfig_exports = {};
__export(getDefaultNativeAuthConfig_exports, {
  getDefaultNativeAuthConfig: () => getDefaultNativeAuthConfig
});
module.exports = __toCommonJS(getDefaultNativeAuthConfig_exports);
var import_selectors = require("../../../store/selectors");
var import_store = require("../../../store/store");
var import_getWindowLocation = require("../../../utils/window/getWindowLocation");
const DEFAULT_API_ADDRESS = "https://api.multiversx.com";
const DEFAULT_EXPIRY_SECONDS = 60 * 60 * 24;
const DEFAULT_TOKEN_EXPIRATION_TOAST_WARNING_SECONDS = 5 * 60;
const getDefaultNativeAuthConfig = (config) => {
  const network = (0, import_selectors.networkSelector)((0, import_store.getState)());
  return {
    origin: config?.origin || (0, import_getWindowLocation.getWindowLocation)().origin,
    apiAddress: config?.apiAddress || network.apiAddress || DEFAULT_API_ADDRESS,
    expirySeconds: config?.expirySeconds || DEFAULT_EXPIRY_SECONDS,
    tokenExpirationToastWarningSeconds: config?.tokenExpirationToastWarningSeconds || DEFAULT_TOKEN_EXPIRATION_TOAST_WARNING_SECONDS
  };
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getDefaultNativeAuthConfig
});
//# sourceMappingURL=getDefaultNativeAuthConfig.cjs.map
