import { UITagsEnum } from '../constants/UITags.enum';
import type { IEventBus } from '../lib/sdkDappUi';
export interface CreateEventBusUIElementType extends HTMLElement {
    getEventBus: () => Promise<IEventBus | null>;
}
export type CreateComponentType = <T = CreateEventBusUIElementType>({ name, anchor }: {
    name: `${UITagsEnum}`;
    anchor?: HTMLElement;
}) => Promise<T>;
export declare class ComponentFactory {
    private static createComponent;
    static get create(): CreateComponentType;
    static set create(customCreateFunction: CreateComponentType);
}
