import { TransactionDirectionEnum, ServerTransactionType } from '../../../../types/serverTransactions.types';
interface IGetTransactionTransferTypeParams {
    address: string;
    transaction: ServerTransactionType;
    receiver: string;
}
export declare function getTransactionTransferType({ address, transaction, receiver }: IGetTransactionTransferTypeParams): TransactionDirectionEnum;
export {};
