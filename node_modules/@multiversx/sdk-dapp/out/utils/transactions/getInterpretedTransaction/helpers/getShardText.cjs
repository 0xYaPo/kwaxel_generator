"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getShardText_exports = {};
__export(getShardText_exports, {
  getShardText: () => getShardText
});
module.exports = __toCommonJS(getShardText_exports);
var import_constants = require("../../../../constants/index");
const getShardText = (shard) => {
  let shardText = shard;
  if (typeof shardText === "string" && shardText.includes("Shard")) {
    shardText = shardText.replace("Shard", "").replace(" ", "");
  }
  const isMetachain = import_constants.METACHAIN_SHARD_ID.toString() === String(shardText).toString() || String(shardText) === "metachain";
  const isAllShards = import_constants.ALL_SHARDS_SHARD_ID.toString() === String(shardText).toString();
  if (isMetachain) {
    return "Metachain";
  }
  if (isAllShards) {
    return "All Shards";
  }
  return `Shard ${shardText}`;
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getShardText
});
//# sourceMappingURL=getShardText.cjs.map
