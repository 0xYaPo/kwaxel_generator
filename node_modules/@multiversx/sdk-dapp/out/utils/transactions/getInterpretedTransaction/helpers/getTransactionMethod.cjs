"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getTransactionMethod_exports = {};
__export(getTransactionMethod_exports, {
  getTransactionMethod: () => getTransactionMethod
});
module.exports = __toCommonJS(getTransactionMethod_exports);
var import_serverTransactions = require("../../../../types/serverTransactions.types");
const getTransactionMethod = (transaction) => {
  let transactionAction = "Transaction";
  const transactionHasAction = transaction.action?.name && transaction.action?.category;
  if (transactionHasAction) {
    if (transaction.action?.category === import_serverTransactions.TransactionActionCategoryEnum.esdtNft && transaction.action?.name === import_serverTransactions.TransactionActionsEnum.transfer) {
      transactionAction = "Transaction";
    } else if (transaction.action) {
      transactionAction = transaction.action.name;
    }
    if (transaction.action?.arguments?.functionName) {
      transactionAction = transaction.action.arguments.functionName;
    }
  }
  return transactionAction;
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getTransactionMethod
});
//# sourceMappingURL=getTransactionMethod.cjs.map
