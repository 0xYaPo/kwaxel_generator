"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getTransactionTransferType_exports = {};
__export(getTransactionTransferType_exports, {
  getTransactionTransferType: () => getTransactionTransferType
});
module.exports = __toCommonJS(getTransactionTransferType_exports);
var import_serverTransactions = require("../../../../types/serverTransactions.types");
function getTransactionTransferType({
  address,
  transaction,
  receiver
}) {
  const directionOut = address === transaction.sender;
  const directionIn = address === receiver;
  const directionSelf = directionOut && directionIn;
  const isScResult = transaction?.type === import_serverTransactions.TransferTypeEnum.SmartContractResult;
  switch (true) {
    case isScResult:
      return import_serverTransactions.TransactionDirectionEnum.INTERNAL;
    case directionSelf:
      return import_serverTransactions.TransactionDirectionEnum.SELF;
    case directionIn:
      return import_serverTransactions.TransactionDirectionEnum.IN;
    case directionOut:
    default:
      return import_serverTransactions.TransactionDirectionEnum.OUT;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getTransactionTransferType
});
//# sourceMappingURL=getTransactionTransferType.cjs.map
