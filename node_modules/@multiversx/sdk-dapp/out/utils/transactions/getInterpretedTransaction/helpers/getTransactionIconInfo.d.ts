import { ServerTransactionType } from '../../../../types/serverTransactions.types';
export declare const getTransactionIconInfo: (transaction: ServerTransactionType) => {
    icon: string;
    tooltip: string;
};
