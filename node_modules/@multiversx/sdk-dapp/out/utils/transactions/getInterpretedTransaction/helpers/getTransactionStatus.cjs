"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getTransactionStatus_exports = {};
__export(getTransactionStatus_exports, {
  getTransactionStatus: () => getTransactionStatus
});
module.exports = __toCommonJS(getTransactionStatus_exports);
var import_enums = require("../../../../types/enums.types");
function getTransactionStatus(transaction) {
  const statusIs = (compareTo) => transaction.status.toLowerCase() === compareTo.toLowerCase();
  const failed = statusIs(import_enums.TransactionServerStatusesEnum.fail) || statusIs(import_enums.TransactionServerStatusesEnum.rewardReverted);
  const success = statusIs(import_enums.TransactionServerStatusesEnum.success);
  const invalid = statusIs(import_enums.TransactionServerStatusesEnum.notExecuted) || statusIs(import_enums.TransactionServerStatusesEnum.invalid);
  const pending = statusIs(import_enums.TransactionServerStatusesEnum.pending) || transaction.pendingResults;
  return {
    failed,
    success,
    invalid,
    pending
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getTransactionStatus
});
//# sourceMappingURL=getTransactionStatus.cjs.map
