"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getTransactionActionDirectionLabel_exports = {};
__export(getTransactionActionDirectionLabel_exports, {
  getTransactionActionDirectionLabel: () => getTransactionActionDirectionLabel
});
module.exports = __toCommonJS(getTransactionActionDirectionLabel_exports);
var import_tokens = require("../../../../types/tokens.types");
var import_getReceiverData = require("./getReceiverData");
const getTransactionActionDirectionLabel = ({
  transaction,
  transferType,
  currentUserAddress
}) => {
  const { receiver } = (0, import_getReceiverData.getReceiverData)(transaction);
  const isSelfTransaction = receiver === transaction.sender;
  const isReceiverTransaction = receiver === currentUserAddress;
  const isSenderTransaction = !isSelfTransaction && !isReceiverTransaction;
  const processedTransferType = transferType;
  const selfTransactionTransferTypes = [
    import_tokens.NftEnumType.SemiFungibleESDT,
    import_tokens.NftEnumType.NonFungibleESDT,
    import_tokens.NftEnumType.MetaESDT
  ];
  const isSelfSentTransactionTransfer = selfTransactionTransferTypes.includes(
    processedTransferType
  );
  if (isSelfTransaction && isSelfSentTransactionTransfer || isReceiverTransaction) {
    return "Received";
  }
  if (isSelfTransaction && !isSelfSentTransactionTransfer) {
    return "Self Sent";
  }
  if (isSenderTransaction) {
    return "Sent";
  }
  return "Self";
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getTransactionActionDirectionLabel
});
//# sourceMappingURL=getTransactionActionDirectionLabel.cjs.map
