import { AssetType } from '../../../../types/account.types';
import { ServerTransactionType } from '../../../../types/serverTransactions.types';
export declare const getReceiverData: (transaction: ServerTransactionType) => {
    receiver: string;
    receiverAssets: AssetType;
};
