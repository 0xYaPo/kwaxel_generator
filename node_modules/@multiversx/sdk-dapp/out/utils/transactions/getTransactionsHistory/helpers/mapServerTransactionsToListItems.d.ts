import { ITransactionListItem } from '../../../../lib/sdkDappUi';
import { SignedTransactionType } from '../../../../types/transactions.types';
interface IMapServerTransactionsToListItemsParams {
    transactions: SignedTransactionType[];
    address: string;
    explorerAddress: string;
    egldLabel: string;
}
export declare const mapServerTransactionsToListItems: ({ transactions, address, explorerAddress, egldLabel }: IMapServerTransactionsToListItemsParams) => Promise<ITransactionListItem[]>;
export {};
