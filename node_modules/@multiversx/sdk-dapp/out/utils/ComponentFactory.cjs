"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ComponentFactory_exports = {};
__export(ComponentFactory_exports, {
  ComponentFactory: () => ComponentFactory
});
module.exports = __toCommonJS(ComponentFactory_exports);
var import_constants = require("../constants/index");
class ComponentFactory {
  static get create() {
    return this.createComponent;
  }
  static set create(customCreateFunction) {
    this.createComponent = customCreateFunction;
  }
}
ComponentFactory.createComponent = async ({
  name,
  anchor
}) => {
  const webComponent = import_constants.safeWindow?.customElements?.get(name);
  if (!webComponent) {
    return null;
  }
  const element = import_constants.safeWindow.document.createElement(name);
  const rootElement = anchor || import_constants.safeWindow.document.body;
  rootElement.appendChild(element);
  await customElements.whenDefined(name);
  return element;
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ComponentFactory
});
//# sourceMappingURL=ComponentFactory.cjs.map
