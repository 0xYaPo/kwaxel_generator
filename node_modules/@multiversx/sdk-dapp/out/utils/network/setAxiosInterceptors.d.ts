interface InterceptAxiosCallsConfig {
    authenticatedDomains: string[];
    bearerToken?: string;
}
export declare const setAxiosInterceptors: ({ authenticatedDomains, bearerToken }: InterceptAxiosCallsConfig) => void;
export {};
