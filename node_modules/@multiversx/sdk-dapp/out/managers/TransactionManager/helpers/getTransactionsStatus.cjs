"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getTransactionsStatus_exports = {};
__export(getTransactionsStatus_exports, {
  getTransactionsSessionStatus: () => getTransactionsSessionStatus
});
module.exports = __toCommonJS(getTransactionsStatus_exports);
var import_transactionStateByStatus = require("../../../store/actions/transactions/transactionStateByStatus");
var import_types = require("../../../types");
function getTransactionsSessionStatus(transactions) {
  if (!transactions || transactions.length === 0) {
    return import_types.TransactionBatchStatusesEnum.invalid;
  }
  const areAllSuccessful = transactions.every(
    (transaction) => (0, import_transactionStateByStatus.getIsTransactionSuccessful)(transaction.status)
  );
  if (areAllSuccessful) {
    return import_types.TransactionBatchStatusesEnum.success;
  }
  const areAnyFailed = transactions.some(
    (transaction) => (0, import_transactionStateByStatus.getIsTransactionFailed)(transaction.status)
  );
  if (areAnyFailed) {
    return import_types.TransactionBatchStatusesEnum.fail;
  }
  const areAllNotExecuted = transactions.every(
    (transaction) => (0, import_transactionStateByStatus.getIsTransactionNotExecuted)(transaction.status)
  );
  if (areAllNotExecuted) {
    return import_types.TransactionBatchStatusesEnum.invalid;
  }
  return null;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getTransactionsSessionStatus
});
//# sourceMappingURL=getTransactionsStatus.cjs.map
