import { ITransactionListItem } from '../../../../lib/sdkDappUi';
import { TransactionServerStatusesEnum } from '../../../../types/enums.types';
import { TransactionsDisplayInfoType } from '../../../../types/transactions.types';
import { ITransactionToast } from '../types/toast.types';
interface CreateTransactionToastParamsType {
    toastId: string;
    address: string;
    status: TransactionServerStatusesEnum;
    transactions: ITransactionListItem[];
    transactionsDisplayInfo?: TransactionsDisplayInfoType;
    explorerAddress: string;
    startTime: number;
    endTime: number;
}
export declare const createTransactionToast: ({ toastId, address, status, transactions, transactionsDisplayInfo, startTime, endTime }: CreateTransactionToastParamsType) => ITransactionToast;
export {};
