"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getLatestNonce_exports = {};
__export(getLatestNonce_exports, {
  getLatestNonce: () => getLatestNonce
});
module.exports = __toCommonJS(getLatestNonce_exports);
var import_accountSelectors = require("../../store/selectors/accountSelectors");
var import_transactionsSelector = require("../../store/selectors/transactionsSelector");
var import_store = require("../../store/store");
function getLatestNonce(apiAccount) {
  const state = (0, import_store.getState)();
  const transactionsSessions = (0, import_transactionsSelector.transactionsSliceSelector)(state);
  const currentAccountNonce = (0, import_accountSelectors.accountNonceSelector)(state);
  let lastTransactionNonce = Object.keys(transactionsSessions).map(Number).reduce((maxNonce, timestamp) => {
    const transactions = transactionsSessions[timestamp]?.transactions || [];
    return Math.max(maxNonce, ...transactions.map((tx) => tx.nonce), 0);
  }, 0);
  if (lastTransactionNonce > 0) {
    lastTransactionNonce += 1;
  }
  if (apiAccount == null) {
    const currentStoreNonce = Math.max(
      lastTransactionNonce,
      currentAccountNonce
    );
    return currentStoreNonce;
  }
  const currentNonce = Math.max(
    lastTransactionNonce,
    apiAccount.nonce,
    currentAccountNonce
  );
  return currentNonce;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getLatestNonce
});
//# sourceMappingURL=getLatestNonce.cjs.map
