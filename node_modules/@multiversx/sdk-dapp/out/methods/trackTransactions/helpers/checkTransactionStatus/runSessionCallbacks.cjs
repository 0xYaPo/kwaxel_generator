"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var runSessionCallbacks_exports = {};
__export(runSessionCallbacks_exports, {
  runSessionCallbacks: () => runSessionCallbacks
});
module.exports = __toCommonJS(runSessionCallbacks_exports);
var import_sessionCallbacks = require("../../../../managers/TransactionManager/helpers/sessionCallbacks");
var import_enums = require("../../../../types/enums.types");
async function runSessionCallbacks({
  sessionId,
  status
}) {
  const { onSuccess, onFail } = import_sessionCallbacks.sessionCallbacksMap;
  if (!onSuccess || !onFail || status == null) {
    return;
  }
  switch (status) {
    case import_enums.TransactionBatchStatusesEnum.success:
      await onSuccess?.(sessionId);
      break;
    case import_enums.TransactionBatchStatusesEnum.fail:
    case import_enums.TransactionBatchStatusesEnum.cancelled:
    case import_enums.TransactionBatchStatusesEnum.timedOut:
    case import_enums.TransactionBatchStatusesEnum.invalid:
      await onFail?.(sessionId);
      break;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  runSessionCallbacks
});
//# sourceMappingURL=runSessionCallbacks.cjs.map
