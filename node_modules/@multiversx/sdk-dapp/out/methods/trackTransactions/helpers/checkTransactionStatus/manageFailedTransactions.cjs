"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var manageFailedTransactions_exports = {};
__export(manageFailedTransactions_exports, {
  manageFailedTransactions: () => manageFailedTransactions
});
module.exports = __toCommonJS(manageFailedTransactions_exports);
var import_transactionsActions = require("../../../../store/actions/transactions/transactionsActions");
var import_enums = require("../../../../types/enums.types");
var import_runSessionCallbacks = require("./runSessionCallbacks");
async function manageFailedTransactions({
  results,
  hash,
  sessionId
}) {
  const resultWithError = results?.find(
    (scResult) => scResult?.returnMessage !== ""
  );
  (0, import_transactionsActions.updateTransactionStatus)({
    sessionId,
    transaction: {
      ...resultWithError,
      hash,
      status: import_enums.TransactionServerStatusesEnum.fail,
      inTransit: false
    }
  });
  (0, import_transactionsActions.updateSessionStatus)({
    sessionId,
    status: import_enums.TransactionBatchStatusesEnum.fail,
    errorMessage: resultWithError?.returnMessage
  });
  await (0, import_runSessionCallbacks.runSessionCallbacks)({
    sessionId,
    status: import_enums.TransactionBatchStatusesEnum.fail
  });
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  manageFailedTransactions
});
//# sourceMappingURL=manageFailedTransactions.cjs.map
