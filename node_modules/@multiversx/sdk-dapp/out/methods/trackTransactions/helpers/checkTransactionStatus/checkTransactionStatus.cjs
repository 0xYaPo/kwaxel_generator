"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var checkTransactionStatus_exports = {};
__export(checkTransactionStatus_exports, {
  checkTransactionStatus: () => checkTransactionStatus
});
module.exports = __toCommonJS(checkTransactionStatus_exports);
var import_transactionsSelector = require("../../../../store/selectors/transactionsSelector");
var import_store = require("../../../../store/store");
var import_checkBatch = require("./checkBatch");
async function checkTransactionStatus() {
  const pendingSessions = (0, import_transactionsSelector.pendingTransactionsSessionsSelector)((0, import_store.getState)());
  if (Object.keys(pendingSessions).length > 0) {
    for (const [sessionId, { transactions }] of Object.entries(
      pendingSessions
    )) {
      await (0, import_checkBatch.checkBatch)({
        sessionId,
        transactionBatch: transactions
      });
    }
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  checkTransactionStatus
});
//# sourceMappingURL=checkTransactionStatus.cjs.map
