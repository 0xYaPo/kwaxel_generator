export declare const getAuthTokenText: ({ loginToken, version }: {
    loginToken?: string;
    version?: string;
}) => {
    data: string;
    confirmAddressText: string;
    authText: string;
} | null;
