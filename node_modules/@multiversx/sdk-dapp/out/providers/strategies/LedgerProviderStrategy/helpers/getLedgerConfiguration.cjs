"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getLedgerConfiguration_exports = {};
__export(getLedgerConfiguration_exports, {
  getLedgerConfiguration: () => getLedgerConfiguration
});
module.exports = __toCommonJS(getLedgerConfiguration_exports);
var import_constants = require("../../../../constants/index");
async function getLedgerConfiguration(initializedHwWalletP) {
  if (!initializedHwWalletP.isInitialized()) {
    throw new Error("Unable to get version. Provider not initialized");
  }
  const hwApp = initializedHwWalletP.hwApp;
  const { contractData, version } = await hwApp.getAppConfiguration();
  const dataEnabled = contractData === import_constants.LEDGER_CONTRACT_DATA_ENABLED_VALUE;
  return { version, dataEnabled };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getLedgerConfiguration
});
//# sourceMappingURL=getLedgerConfiguration.cjs.map
