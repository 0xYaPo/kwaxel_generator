import { Message } from '../../../../lib/sdkCore';
export declare function signLedgerMessage({ message, handleSignMessage }: {
    message: Message;
    handleSignMessage: (msg: Message) => Promise<Message>;
}): Promise<Message>;
