import { HWProvider } from '@multiversx/sdk-hw-provider';
export declare function getLedgerProvider(props?: {
    shouldInitProvider?: boolean;
}): Promise<{
    ledgerProvider: HWProvider;
    ledgerConfig: {
        version: string;
        dataEnabled: boolean;
    };
}>;
