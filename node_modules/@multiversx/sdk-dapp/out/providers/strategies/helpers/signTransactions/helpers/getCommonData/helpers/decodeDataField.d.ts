import { DecodeMethodEnum, DecodedDisplayType } from '../../../../../../../types/serverTransactions.types';
export declare const getAllDecodedFormats: ({ data, highlight, identifier }: {
    data: string;
    highlight: string | null;
    identifier?: string;
}) => Partial<Record<DecodeMethodEnum, DecodedDisplayType>>;
