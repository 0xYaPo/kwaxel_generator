"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getPpuOptions_exports = {};
__export(getPpuOptions_exports, {
  getPpuOptions: () => getPpuOptions
});
module.exports = __toCommonJS(getPpuOptions_exports);
var import_bignumber = __toESM(require("bignumber.js"));
var import_placeholders = require("../../../../../../../constants/placeholders.constants");
var import_getRecommendedGasPrice = require("./getRecommendedGasPrice");
const getPpuOptions = ({
  shard,
  gasStationMetadata,
  transaction,
  initialGasPrice = 0
}) => {
  const fastPpu = gasStationMetadata ? gasStationMetadata[Number(shard)]?.fast : import_placeholders.EMPTY_PPU;
  const fasterPpu = gasStationMetadata ? gasStationMetadata[Number(shard)]?.faster : import_placeholders.EMPTY_PPU;
  const fastGasPrice = (0, import_getRecommendedGasPrice.getRecommendedGasPrice)({
    transaction,
    gasPriceData: {
      initialGasPrice,
      ppu: fastPpu
    }
  });
  const fasterGasPrice = (0, import_getRecommendedGasPrice.getRecommendedGasPrice)({
    transaction,
    gasPriceData: {
      initialGasPrice,
      ppu: fasterPpu
    }
  });
  const isFastGasPrice = new import_bignumber.default(fastGasPrice).isGreaterThan(
    initialGasPrice || 0
  );
  const isFasterGasPrice = new import_bignumber.default(fasterGasPrice).isGreaterThan(
    initialGasPrice || 0
  );
  const isEditingGasPriceEnabled = isFastGasPrice || isFasterGasPrice;
  if (!isEditingGasPriceEnabled) {
    return [];
  }
  const ppuOptions = [
    {
      label: "Standard",
      value: import_placeholders.EMPTY_PPU
    }
  ];
  if (isFastGasPrice) {
    ppuOptions.push({
      label: "Fast",
      value: fastPpu
    });
  }
  if (isFasterGasPrice) {
    ppuOptions.push({
      label: "Faster",
      value: fasterPpu
    });
  }
  return ppuOptions;
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getPpuOptions
});
//# sourceMappingURL=getPpuOptions.cjs.map
