import { Transaction } from '../../../../../../lib/sdkCore';
export declare const getTransactionsNeedGuardianSigning: ({ transactions, isGuarded }: {
    transactions: Transaction[];
    isGuarded?: boolean;
}) => boolean;
