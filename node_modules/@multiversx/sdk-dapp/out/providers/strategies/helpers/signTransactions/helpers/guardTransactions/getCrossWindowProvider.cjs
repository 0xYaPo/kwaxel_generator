"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getCrossWindowProvider_exports = {};
__export(getCrossWindowProvider_exports, {
  getCrossWindowProvider: () => getCrossWindowProvider
});
module.exports = __toCommonJS(getCrossWindowProvider_exports);
var import_sdkWebWalletCrossWindowProvider = require("../../../../../../lib/sdkWebWalletCrossWindowProvider");
async function getCrossWindowProvider({
  address,
  walletUrl
}) {
  try {
    const provider = import_sdkWebWalletCrossWindowProvider.CrossWindowProvider.getInstance();
    const success = await provider.init();
    provider.setAddress(address).setWalletUrl(walletUrl);
    if (success) {
      return provider;
    } else {
      console.error("Could not initialize CrossWindowWallet Provider");
    }
  } catch (err) {
    console.error("Unable to login to CrossWindowWallet Provider", err);
  }
  return null;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getCrossWindowProvider
});
//# sourceMappingURL=getCrossWindowProvider.cjs.map
