"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var guardTransactions_exports = {};
__export(guardTransactions_exports, {
  guardTransactions: () => guardTransactions
});
module.exports = __toCommonJS(guardTransactions_exports);
var import_getAccount = require("../../../../../../methods/account/getAccount");
var import_selectors = require("../../../../../../store/selectors");
var import_store = require("../../../../../../store/store");
var import_getCrossWindowProvider = require("./getCrossWindowProvider");
var import_getTransactionsNeedGuardianSigning = require("./getTransactionsNeedGuardianSigning");
const guardTransactions = async (transactions) => {
  const { isGuarded } = (0, import_getAccount.getAccount)();
  const needs2FAsigning = (0, import_getTransactionsNeedGuardianSigning.getTransactionsNeedGuardianSigning)({
    isGuarded,
    transactions
  });
  if (!needs2FAsigning) {
    return transactions;
  }
  const sender = transactions[0].sender.toBech32();
  const { walletAddress } = (0, import_selectors.networkSelector)((0, import_store.getState)());
  const provider = await (0, import_getCrossWindowProvider.getCrossWindowProvider)({
    address: sender,
    walletUrl: walletAddress
  });
  const guardedTransactions = await provider?.guardTransactions(transactions);
  return guardedTransactions || [];
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  guardTransactions
});
//# sourceMappingURL=guardTransactions.cjs.map
