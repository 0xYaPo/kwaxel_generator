import { Transaction } from '../../../../../lib/sdkCore';
export declare const getFeeData: ({ transaction, price }: {
    transaction: Transaction;
    price?: number;
}) => {
    feeLimitFormatted: string;
    feeInFiatLimit: string | null;
    feeLimit: string;
};
