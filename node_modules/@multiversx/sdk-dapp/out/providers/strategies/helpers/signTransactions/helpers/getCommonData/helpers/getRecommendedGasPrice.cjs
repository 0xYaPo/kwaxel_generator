"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getRecommendedGasPrice_exports = {};
__export(getRecommendedGasPrice_exports, {
  getRecommendedGasPrice: () => getRecommendedGasPrice
});
module.exports = __toCommonJS(getRecommendedGasPrice_exports);
var import_sdkDappUtils = require("../../../../../../../lib/sdkDappUtils");
function getRecommendedGasPrice({
  transaction,
  gasPriceData
}) {
  if (!gasPriceData) {
    throw new Error("Gas price not found for nonce: " + transaction.nonce);
  }
  const { initialGasPrice, ppu } = gasPriceData;
  const newGasPrice = ppu ? (0, import_sdkDappUtils.recommendGasPrice)({
    transactionDataLength: String(transaction?.data || "").length,
    transactionGasLimit: transaction.gasLimit,
    ppu
  }) : initialGasPrice;
  return newGasPrice;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getRecommendedGasPrice
});
//# sourceMappingURL=getRecommendedGasPrice.cjs.map
