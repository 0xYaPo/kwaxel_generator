import { DappProvider } from './DappProvider/DappProvider';
import { ICustomProvider, IProviderFactory } from './types/providerFactory.types';
export declare class ProviderFactory {
    private static _customProviders;
    static set customProviders(providers: ICustomProvider[]);
    static get customProviders(): ICustomProvider[];
    static create({ type, anchor }: IProviderFactory): Promise<DappProvider>;
    static destroy(): Promise<void>;
}
