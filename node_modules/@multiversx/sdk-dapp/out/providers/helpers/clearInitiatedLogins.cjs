"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var clearInitiatedLogins_exports = {};
__export(clearInitiatedLogins_exports, {
  clearInitiatedLogins: () => clearInitiatedLogins
});
module.exports = __toCommonJS(clearInitiatedLogins_exports);
var import_sdkWebWalletCrossWindowProvider = require("../../lib/sdkWebWalletCrossWindowProvider");
var import_sdkWebWalletIframeProvider = require("../../lib/sdkWebWalletIframeProvider");
var import_providerFactory = require("../types/providerFactory.types");
const clearInitiatedLogins = (props) => {
  Object.values(import_providerFactory.ProviderTypeEnum).forEach((method) => {
    if (method === props?.skipLoginMethod) {
      return;
    }
    switch (method) {
      case import_providerFactory.ProviderTypeEnum.crossWindow: {
        const crossWindowProvider = import_sdkWebWalletCrossWindowProvider.CrossWindowProvider.getInstance();
        if (crossWindowProvider.isInitialized()) {
          crossWindowProvider.onDestroy();
        }
        break;
      }
      case import_providerFactory.ProviderTypeEnum.metamask:
      case import_providerFactory.ProviderTypeEnum.passkey: {
        const iframeProvider = import_sdkWebWalletIframeProvider.IframeProvider.getInstance();
        if (iframeProvider.isInitialized()) {
          iframeProvider.dispose();
        }
        break;
      }
      default:
        break;
    }
  });
  return null;
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  clearInitiatedLogins
});
//# sourceMappingURL=clearInitiatedLogins.cjs.map
