import { IProvider } from '../../../../providers/types/providerFactory.types';
export declare function login(provider: IProvider, options?: {
    token?: string;
}): Promise<{
    address: string;
    signature: string;
} | {
    address: string;
} | null>;
