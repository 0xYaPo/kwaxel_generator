"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var extractAddressFromToken_exports = {};
__export(extractAddressFromToken_exports, {
  extractAddressFromToken: () => extractAddressFromToken
});
module.exports = __toCommonJS(extractAddressFromToken_exports);
var import_loginInfoActions = require("../../../../../store/actions/loginInfo/loginInfoActions");
var import_getAccountFromToken = require("./getAccountFromToken");
async function extractAddressFromToken({
  loginToken,
  extraInfoData,
  address
}) {
  const accountDetails = await (0, import_getAccountFromToken.getAccountFromToken)({
    originalLoginToken: loginToken,
    extraInfoData,
    address
  });
  if (accountDetails.modifiedLoginToken) {
    (0, import_loginInfoActions.setLoginToken)(accountDetails.modifiedLoginToken);
  }
  return accountDetails.address;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  extractAddressFromToken
});
//# sourceMappingURL=extractAddressFromToken.cjs.map
