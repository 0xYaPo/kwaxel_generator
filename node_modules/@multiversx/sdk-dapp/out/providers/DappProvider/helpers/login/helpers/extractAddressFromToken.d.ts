interface IExtractAccountFromTokenProps {
    loginToken: string;
    extraInfoData: {
        multisig?: string;
        impersonate?: string;
    };
    address: string;
}
export declare function extractAddressFromToken({ loginToken, extraInfoData, address }: IExtractAccountFromTokenProps): Promise<string>;
export {};
