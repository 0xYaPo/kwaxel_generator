"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getAccountFromToken_exports = {};
__export(getAccountFromToken_exports, {
  getAccountFromToken: () => getAccountFromToken
});
module.exports = __toCommonJS(getAccountFromToken_exports);
var import_getModifiedLoginToken = require("./getModifiedLoginToken");
async function getAccountFromToken({
  originalLoginToken,
  extraInfoData,
  address
}) {
  const modifiedLoginToken = await (0, import_getModifiedLoginToken.getModifiedLoginToken)({
    loginToken: originalLoginToken,
    extraInfoData
  });
  const tokenAddress = extraInfoData.multisig || extraInfoData.impersonate || address;
  const accountAddress = modifiedLoginToken != null ? tokenAddress : address;
  return {
    address: accountAddress,
    modifiedLoginToken
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getAccountFromToken
});
//# sourceMappingURL=getAccountFromToken.cjs.map
