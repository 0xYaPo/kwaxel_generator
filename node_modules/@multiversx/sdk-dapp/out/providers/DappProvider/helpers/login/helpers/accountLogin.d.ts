import { IProvider } from '../../../../../providers/types/providerFactory.types';
interface IAccountLoginProps {
    address: string;
    provider: IProvider;
    apiAddress: string;
}
export declare function accountLogin({ address, provider, apiAddress }: IAccountLoginProps): Promise<void>;
export {};
