import { Transaction } from '../../../../lib/sdkCore';
export declare const computeNonces: ({ latestNonce, transactions }: {
    latestNonce: number;
    transactions: Array<Transaction>;
}) => Array<Transaction>;
