"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var signMessageWithProvider_exports = {};
__export(signMessageWithProvider_exports, {
  signMessageWithProvider: () => signMessageWithProvider
});
module.exports = __toCommonJS(signMessageWithProvider_exports);
var import_sdkCore = require("../../../../lib/sdkCore");
var import_getAddress = require("../../../../methods/account/getAddress");
async function signMessageWithProvider({
  message,
  provider,
  options
}) {
  const address = (0, import_getAddress.getAddress)();
  const messageToSign = new import_sdkCore.Message({
    address: new import_sdkCore.Address(address),
    data: message.data
  });
  const signedMessage = await provider.signMessage(messageToSign, options);
  return signedMessage;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  signMessageWithProvider
});
//# sourceMappingURL=signMessageWithProvider.cjs.map
