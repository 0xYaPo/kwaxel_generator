import { Message } from '../../../../lib/sdkCore';
import { IProvider } from '../../../../providers/types/providerFactory.types';
export type SignMessageType = {
    provider: IProvider;
    message: Message;
    options?: {
        hasConsentPopup?: boolean;
    };
};
export declare function signMessageWithProvider({ message, provider, options }: SignMessageType): Promise<Message | null>;
