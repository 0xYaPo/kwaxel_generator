"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getPersistedToken_exports = {};
__export(getPersistedToken_exports, {
  getPersistedToken: () => getPersistedToken
});
module.exports = __toCommonJS(getPersistedToken_exports);
var import_axiosInstance = require("../../apiCalls/utils/axiosInstance");
var import_network = require("../../constants/network.constants");
var import_tokenDataStorage = require("./tokenDataStorage");
async function getPersistedToken(url) {
  const config = {
    timeout: import_network.TIMEOUT
  };
  const cachedToken = await import_tokenDataStorage.tokenDataStorage.getItem(url);
  if (cachedToken) {
    return cachedToken;
  }
  const response = await import_axiosInstance.axiosInstance.get(url, config);
  await import_tokenDataStorage.tokenDataStorage.setItem(url, response.data);
  return response.data;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getPersistedToken
});
//# sourceMappingURL=getPersistedToken.cjs.map
