import { StoreType } from '../../store/store.types';
export declare const uiSelector: ({ ui }: StoreType) => import("../slices/ui/ui.types").UiSliceType;
export declare const isSidePanelOpenSelector: ({ ui }: StoreType) => boolean;
