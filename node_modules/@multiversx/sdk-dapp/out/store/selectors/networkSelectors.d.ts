import { StoreType } from '../../store/store.types';
export declare const networkConfigSelector: ({ network }: StoreType) => import("../slices/network/networkSlice.types").NetworkSliceType;
export declare const networkSelector: ({ network }: StoreType) => import("../..").NetworkType;
export declare const chainIdSelector: ({ network: { network } }: StoreType) => string;
export declare const walletAddressSelector: ({ network: { network } }: StoreType) => string;
export declare const roundDurationSelectorSelector: ({ network: { network } }: StoreType) => number;
