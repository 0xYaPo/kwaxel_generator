import { StoreType } from '../../store/store.types';
export declare const cacheSliceSelector: ({ cache }: StoreType) => import("../slices/cache/cacheSlice.types").CacheSliceType;
export declare const getCachedItemSelector: <T>(key: string) => ({ cache }: StoreType) => T | undefined;
