"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var configActions_exports = {};
__export(configActions_exports, {
  setNativeAuthConfig: () => setNativeAuthConfig,
  setWalletConnectConfig: () => setWalletConnectConfig,
  setWebsocketStatus: () => setWebsocketStatus
});
module.exports = __toCommonJS(configActions_exports);
var import_walletConnect = require("../../../constants/walletConnect.constants");
var import_store = require("../../../store/store");
const setNativeAuthConfig = (config) => (0, import_store.getStore)().setState(
  ({ config: state }) => {
    state.nativeAuthConfig = config;
  },
  false,
  "setNativeAuthConfig"
);
const setWebsocketStatus = (status) => (0, import_store.getStore)().setState(
  ({ config: state }) => {
    state.websocketStatus = status;
  },
  false,
  "setWebsocketStatus"
);
const setWalletConnectConfig = (config) => (0, import_store.getStore)().setState(
  ({ config: state }) => {
    const walletConnectV2RelayAddress = config.walletConnectV2RelayAddress || import_walletConnect.fallbackWalletConnectConfigurations.walletConnectV2RelayAddress;
    const walletConnectDeepLink = config.walletConnectDeepLink || import_walletConnect.fallbackWalletConnectConfigurations.walletConnectDeepLink;
    state.walletConnectConfig = {
      ...config,
      walletConnectDeepLink,
      walletConnectV2RelayAddress
    };
  },
  false,
  "setWalletConnectConfig"
);
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  setNativeAuthConfig,
  setWalletConnectConfig,
  setWebsocketStatus
});
//# sourceMappingURL=configActions.cjs.map
