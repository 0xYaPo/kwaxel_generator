import { EnvironmentsEnum } from '../../../types/enums.types';
import { NetworkType, CustomNetworkType } from '../../../types/network.types';
export type InitializeNetworkPropsType = {
    customNetworkConfig?: CustomNetworkType;
    environment?: EnvironmentsEnum;
};
export declare const initializeNetwork: ({ customNetworkConfig, environment }: InitializeNetworkPropsType) => Promise<NetworkType>;
