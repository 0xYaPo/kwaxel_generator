"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var sharedActions_exports = {};
__export(sharedActions_exports, {
  loginAction: () => loginAction,
  logoutAction: () => logoutAction
});
module.exports = __toCommonJS(sharedActions_exports);
var import_sdkCore = require("../../../lib/sdkCore");
var import_logoutMiddleware = require("../../../store/middleware/logoutMiddleware");
var import_store = require("../../../store/store");
const logoutAction = () => (0, import_store.getStore)().setState(import_logoutMiddleware.resetStore);
const loginAction = ({
  address,
  providerType
}) => {
  (0, import_store.getStore)().setState(
    ({ account, loginInfo }) => {
      account.address = address;
      account.publicKey = new import_sdkCore.Address(address).toHex();
      if (loginInfo) {
        loginInfo.providerType = providerType;
      }
    },
    false,
    "loginAction"
  );
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  loginAction,
  logoutAction
});
//# sourceMappingURL=sharedActions.cjs.map
