"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseAmount = void 0;
const bignumber_js_1 = __importDefault(require("bignumber.js"));
const constants_1 = require("../constants");
function parseAmount(amount, numDecimals = constants_1.DECIMALS) {
    const amountBN = new bignumber_js_1.default(amount);
    const multiplier = new bignumber_js_1.default(10).pow(numDecimals);
    const result = amountBN.multipliedBy(multiplier);
    if (!result.isInteger()) {
        throw new Error('Amount has too many decimal places');
    }
    return result.toFixed(0);
}
exports.parseAmount = parseAmount;
//# sourceMappingURL=parseAmount.js.map