import { Compiler } from '@stencil/core/compiler';
import { EventEmitter } from 'node:events';

declare class BuildQueue extends EventEmitter {
    #private;
    constructor(compiler: Compiler);
    getLatestBuild(srcPath: string, distPath: string): Promise<string>;
}

export { BuildQueue };
